# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PillPrompt' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PillPrompt
  pod "AlamofireImage"
  pod 'Parse'

  target 'PillPromptTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PillPromptUITests' do
    # Pods for testing
  end

end
